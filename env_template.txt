# -----------------------------------------------------------------------------
# AI Homework Reviewer Environment Variables
#
# Instructions:
# 1. Rename this file to `.env` in the project's root directory.
# 2. Fill in the placeholder values with your actual credentials and settings.
# -----------------------------------------------------------------------------

# -- OpenAI Configuration --
# Your secret API key for accessing OpenAI models.
OPENAI_API_KEY=your_openai_api_key_here
# The specific OpenAI model to use for the review process (e.g., gpt-4, gpt-3.5-turbo).
OPENAI_MODEL=gpt-4

# -- GitHub Configuration --
# A GitHub Personal Access Token (PAT) with 'repo' scope.
# This is required to make authenticated API requests and avoid strict rate limits.
GITHUB_TOKEN=your_github_personal_access_token_here

# -- Local FastAPI Server Settings --
# These are used when running the application locally as a REST API.
DEBUG=false
HOST=0.0.0.0
PORT=8000
HOMEWORK_DIR=homework
RESULTS_DIR=results

# -- AWS Lambda & SES Configuration --
# These variables are required for the serverless deployment on AWS.
# The AWS region where your Lambda function and SES are configured (e.g., us-east-1).
AWS_REGION=your-aws-region-here
# The mount path for the EFS file system within the Lambda environment.
STORAGE_PATH=/mnt/efs
# The email address you have verified with Amazon SES to send notifications FROM.
SENDER_EMAIL=your-verified-sender-email@example.com
# The email address that will receive the weekly summary reports.
RECIPIENT_EMAIL=your-recipient-email@example.com